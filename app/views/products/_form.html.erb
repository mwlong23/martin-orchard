<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :product_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :product_description %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight, id: :product_weight %>
  </div>

  <div class="field">
    <%= form.label :box_price %>
    <%= form.number_field :box_price, id: :product_box_price %>
  </div>

  <div class="field">
    <%= form.label :weekly_deliver_cost %>
    <%= form.number_field :weekly_deliver_cost, id: :product_weekly_deliver_cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
